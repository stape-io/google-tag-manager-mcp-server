---
"gtm-mcp-worker": minor
---

Serve `/llms.txt` describing the server, and pick up the new `gtm_auth_status` and `gtm_guide` tools and the `importFromGallery` / `bulkUpdate` actions from core on both `/mcp` and `/sse`.
